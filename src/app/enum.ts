export enum ClientStatus {
    INITIALISING,
    CHECKEXISTS,
    DOWNLOADCLIENT,
    UPDATEAVAILABLE,
    STARTING,
    RUNNING,
    RUNNINGEXTERNAL,
    STOPPED,
    BOOTSTRAPPING,
    BOOTSTRAPEXTRACTING,
    NOCREDENTIALS,
    INVALIDHASH,
    DOWNLOADFAILED,
    UNSUPPORTEDPLATFORM,
    SHUTTINGDOWN,
    RESTARTING,
    CLOSEDUNEXPECTED,
    BOOTSTRAPFAILED,
    BOOTSTRAPINSUFFICIENTSPACE,
    UNKNOWNERROR
}

export enum ChainType {
    MAINNET,
    TESTNET,
    REGTEST
}

export enum BudgetVote {
    NONE = 0,
    ABSTAIN = 1,
    NO = 2,
    YES = 3,
}