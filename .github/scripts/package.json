{
  "name": "altitude-ci-scripts",
  "private": true,
  "description": "Isolated dependencies for one-off CI helper scripts, kept separate from the app's own package.json/lockfile",
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
